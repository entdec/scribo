main:
  scribable: one (Account)
  properties:
    baseurl: /
    title: main

second:
  scribable: two (Account)
  properties:
    baseurl: /second
    title: second
    email: "email@example.com"
    social_links:
      rss: rss
      github: github

collection:
  properties:
    baseurl: /collection

translate_site:
  properties:
    baseurl: /

empty:
  properties:
    baseurl: /collection
