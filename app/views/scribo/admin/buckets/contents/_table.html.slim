table.table
  thead
    tr
      th Path
      th Name
      th Identifier
      th State
      th Content type
      th Layout
  tbody
    - for content in @contents
      tr
        td = link_to content.path || '-', edit_admin_bucket_content_url(content.bucket, content)
        td = link_to content.name || '-', edit_admin_bucket_content_url(content.bucket, content)
        td = link_to content.identifier || '-', edit_admin_bucket_content_url(content.bucket, content)
        td = content.state
        td = content.content_type
        td = content.layout&.name

- content_for :context_menu do
  =< link_to(t('scribo.context_menu.add'), new_admin_bucket_content_path(@bucket), class: 'btn btn-primary')
